

using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.VisualStudio.TestTools.UnitTesting;


// This is the output code from your template
// you only get syntax-highlighting here - not intellisense
namespace AutomateUnitTest.UnitTest{
  [TestClass]
  public class MyGeneratedClass{
  
			[TestMethod]
			public void Auth_Syncada_10KLevel_Request()
			{
			Program prog = new Program();
            int result = prog.Addition(10, 10);
            int actual = 20;
            Assert.AreEqual<int>(result, actual);
			}

	    
			[TestMethod]
			public void Auth_Syncada_MS_Authority_Request()
			{
			Program prog = new Program();
            int result = prog.Addition(10, 10);
            int actual = 20;
            Assert.AreEqual<int>(result, actual);
			}

	    
			[TestMethod]
			public void Auth_Syncada_ZeroLevel_Request()
			{
			Program prog = new Program();
            int result = prog.Addition(10, 10);
            int actual = 20;
            Assert.AreEqual<int>(result, actual);
			}

	    
			[TestMethod]
			public void Auto_Syncada_eBill_Dollar_Request()
			{
			Program prog = new Program();
            int result = prog.Addition(10, 10);
            int actual = 20;
            Assert.AreEqual<int>(result, actual);
			}

	    
			[TestMethod]
			public void Auto_Syncada_Inv_Collierville_Request()
			{
			Program prog = new Program();
            int result = prog.Addition(10, 10);
            int actual = 20;
            Assert.AreEqual<int>(result, actual);
			}

	    
			[TestMethod]
			public void Auto_Syncada_Inv_Mktg_Request()
			{
			Program prog = new Program();
            int result = prog.Addition(10, 10);
            int actual = 20;
            Assert.AreEqual<int>(result, actual);
			}

	  }
}
